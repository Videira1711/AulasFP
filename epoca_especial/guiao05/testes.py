votes = [15300, 12000, 6600, 5100]
numseats = 6

def hondt(votes, numseats):
    inicial=votes[]
    final = [0] * len(votes)
    for j in range(0,numseats):
        m= max(votes)
        if votes.count(m)>=2:

        


    return final

print(hondt(votes, numseats))
